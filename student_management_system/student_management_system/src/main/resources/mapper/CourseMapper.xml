<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.studentmanager.mapper.CourseMapper">
    <select id="selectById" resultType="com.example.studentmanager.model.entity.CourseEntity">
        SELECT
            id ,
            course_name ,
            credit,
            teacher_id
        FROM course
        WHERE id = #{courseId}
    </select>

    <select id="selectAll" resultType="com.example.studentmanager.model.entity.CourseEntity">
        SELECT
            id AS courseId,
            course_name AS courseName,
            credit,
            teacher_id AS teacherId
        FROM course
    </select>

    <insert id="insert">
        INSERT INTO course (course_name, credit, teacher_id)
        VALUES (#{courseName}, #{credit}, #{teacherId})
    </insert>

    <update id="update">
        UPDATE course
        SET
            course_name = #{courseName},
            credit = #{credit},
            teacher_id = #{teacherId}
        WHERE id = #{courseId}
    </update>

    <select id="selectByCourseName" resultType="com.example.studentmanager.model.entity.CourseEntity">
        SELECT * FROM course
        WHERE course_name = #{courseName}
    </select>

    <delete id="deleteById">
        DELETE FROM course
        WHERE id = #{courseId}
    </delete>
</mapper>