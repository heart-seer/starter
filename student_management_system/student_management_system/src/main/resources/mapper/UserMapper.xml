<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.example.studentmanager.mapper.UserMapper">

    <select id="selectByUsername" parameterType="java.lang.String" resultType="com.example.studentmanager.model.entity.UserEntity">
        SELECT
            id,
            username,
            password,
            role
        FROM user
        WHERE username = #{username}
    </select>

    <select id="selectAll" resultType="com.example.studentmanager.model.entity.UserEntity">
        SELECT
            id,
            username,
            password,
            role
        FROM user
    </select>

    <select id="selectById" resultType="com.example.studentmanager.model.entity.UserEntity">
        SELECT
            id,
            username,
            password,
            role
        FROM user
        WHERE id = #{userId}
    </select>

    <insert id="insert">
        INSERT INTO user (username, password, role)
        VALUES (#{username}, #{password}, #{role})
    </insert>

    <update id="update">
        UPDATE user
        SET
            username = #{username},
            password = #{password},
            role = #{role}
        WHERE id = #{id}
    </update>

    <delete id="deleteByUsername">
        DELETE FROM user
        WHERE username = #{username}
    </delete>
    <update id="updateLoginFailCount">
        update user
        set login_fail_count = #{loginFailCount}
        where id = #{id}
    </update>
</mapper>
